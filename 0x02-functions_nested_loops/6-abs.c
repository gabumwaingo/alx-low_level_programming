#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @i: is the integer whose value is computed
 *
 * Return: Absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
}
