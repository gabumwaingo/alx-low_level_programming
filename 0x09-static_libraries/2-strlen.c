#include "main.h"

/**
 * _strlen - it returns lenth of a string
 * @s: variable containing the string
 *
 * Return: string lenth
 */
int _strlen(char *s)
{
	int a;

	for (a = 0; s[a] != 0; a++)
	{
	}
	return (a);
}
