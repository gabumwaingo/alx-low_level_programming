#include <stdio.h>
#include "main.h"
/**
 * sub - subtracts two ints
 * @a: first int
 * @b: second int
 * Return: subtraction of the two ints
 */
int sub(int a, int b)
{
	int sub;

	s = a - b;
	return (s);
}
