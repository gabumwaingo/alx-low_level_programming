#include "main.h"

/**
 * _puts - prints a string on stdout
 * @str: the string to be printed
 *
 * Return: the printed string
 */
void _puts(char *str)
{
	int a;
	char b;

	for (a = 0; str[a]; a++)
	{
	}
	b = str[a];

	_putchar(b);
}
